# Central validation configuration.
# Read by the reusable workflow on every run.
# Schema: validation/schemas/validation-config-schema.yaml

version: 1

defaults:
  stage: disabled

fork_owners:
  - hdamker
  - rartych

repositories:
  ReleaseTest:
    stage: standard
