## Release Issue Created

This Release Issue was created automatically **due to changes in `release-plan.yaml`**{{#trigger_pr_number}}
introduced by **PR #{{trigger_pr_number}}**{{/trigger_pr_number}}.

<!-- release-automation:workflow-owned -->

**Release:** `{{release_tag}}` (`{{release_type}}`){{#meta_release}} — {{meta_release}}{{/meta_release}}

---

**Next step**: Use `/create-snapshot` to begin the release process.
